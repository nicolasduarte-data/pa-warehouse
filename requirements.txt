# pa-warehouse — Python dependencies
# dbt 1.9+ required for `dbt_valid_to_current` config used in the SCD2
# dim_employee snapshot.

# Core data libraries
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.11,<2.0              # scipy.stats.lognorm for tenure; Hellinger via scipy.spatial.distance
faker>=22.0,<25.0             # string attributes only (names, emails) — not a synthetic data generator
scikit-learn>=1.4,<2.0        # TSTS baseline (logistic regression) + Spearman correlation
statsmodels>=0.14,<1.0        # Progressive OLS for pay equity panel — native p-values, log-linear

# BigQuery client
google-cloud-bigquery>=3.20

# dbt — must be 1.9+ for dbt_valid_to_current snapshot config
dbt-core>=1.9,<2.0
dbt-bigquery>=1.9,<2.0
dbt-snowflake>=1.9,<2.0       # for the Snowflake validation port

# Deliberately NOT used:
#   lifelines  — scipy.stats.lognorm is sufficient (Weibull rejected; log-normal preferred for non-monotone HR hazard)
#   sdmetrics  — replaced by Hellinger + Kaplan-Meier + Spearman + FK assertions in validate.py
